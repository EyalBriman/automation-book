# מדריך למנהל הקורס: שינוי והוספת שאלות ב־Word

המדריך מיועד למי שאינו מתכנת. התשובה הקצרה היא: **עורכים את ה־Word, מריצים
בנייה, בודקים, ורק אז מפרסמים את קובצי האתר שנוצרו.** החלפת Word בלבד או
העלאתו ל־GitHub אינה מעדכנת את האתר.

## מהו קובץ המקור

קובץ ברירת המחדל נמצא כאן:

`private-source/Automation_book4Aug2026.docx`

מומלץ לשמור עותק גיבוי לפני עריכה. אפשר להחליף את הקובץ בגרסה חדשה באותו שם,
או להשאיר אותו במקום אחר ולגרור אותו על `BUILD_SITE_WINDOWS.bat`.

## שינוי מילה, מספר, נוסחה או פתרון קיים

1. פותחים את קובץ ה־Word.
2. משנים את הטקסט במקום הרגיל, בלי למחוק את כותרת השאלה או את פסקת `פתרון`.
3. שומרים וסוגרים את Word.
4. מריצים את סקריפט הבנייה.
5. בודקים בדפדפן את השאלה, הפתרון, השאלה שלפניה והשאלה שאחריה.
6. מפרסמים רק אם בדיקת התקינות הסתיימה בהצלחה.

## הוספת שאלה חדשה — הכלל החשוב

הדרך הבטוחה היא **להעתיק שאלה שלמה מאותו סעיף**, כולל כותרת השאלה, התוכן,
פסקת `פתרון` ותוכן הפתרון. מדביקים מיד אחרי השאלה האחרונה בסעיף, ואז מחליפים
את התוכן. כך נשמרים סגנון Word, רמת הרשימה והמספור שהבונה מזהה.

### פרקים 1–3

1. מסמנים שאלה קיימת מאותו סעיף, מתחילת שורת השאלה ועד סוף הפתרון.
2. מעתיקים ומדביקים אחרי השאלה האחרונה בסעיף.
3. משאירים את שורת הכותרת באותה רמת רשימה מרובת־רמות. אין להקליד מספר שאלה
   ידנית כפסקה רגילה.
4. מחליפים את נוסח השאלה.
5. משאירים פסקה נפרדת שכתוב בה בדיוק `פתרון` או `פתרון:`.
6. מחליפים את תוכן הפתרון ושומרים.

המספר הציבורי מחושב אוטומטית לפי המיקום בסעיף. לדוגמה, שאלה שלישית בסעיף 3.2
תופיע כ־3.2.3. כך התווספה שאלת `mean shift` בגרסת August 2026.

### פרק 4

מעתיקים בלוק שאלה קיים באותו סעיף ומשאירים את צורת הכותרת `1)`, `2)`, `3)`.
מעדכנים את המספר לפי המיקום, ומשאירים `פתרון` בפסקה נפרדת. סעיף 4.8 מוחרג
מהפרסום במכוון; חומר שמוכנס לשם לא יופיע באתר.

### פרק 5 ושאלות מרובות סעיפים

יש להעתיק בחינה או סעיף מלא באותה תבנית. כל סעיף שאלה צריך להיות פריט ממוספר
ב־Word, ואחריו פסקת `פתרון` נפרדת. המערכת נותנת לסעיפים אותיות א, ב, ג וכן
הלאה. הבחינות הקיימות מכילות כמה חריגים היסטוריים שהבונה כבר מכיר; לשאלות
חדשות יש להשתמש בתבנית הרגילה עם `פתרון` לכל סעיף.

## מתי שאלה אינה מתפרסמת

כותרת שאלה בלי פסקת `פתרון` נחשבת טיוטה ואינה עולה לאתר. זהו המצב של שתי
הכותרות הריקות בסעיף 3.3. לאחר שמוסיפים מתחת לכותרת תוכן אמיתי ופסקת `פתרון`,
היא תפורסם בבנייה הבאה.

## תמונות, טבלאות ושרטוטים

- לתמונה חדשה עדיף להשתמש ב־Word באפשרות `In Line with Text` ולא בצורה צפה.
- אפשר להוסיף טבלאות רגילות בתוך השאלה או הפתרון.
- שרטוטים המורכבים מהרבה Word Shapes עלולים להזדקק להשטחה לתמונה אחת.
- אם תמונה חסרה בתוצאה, אין לפרסם; שמרו אותה כ־PNG והכניסו אותה כ־inline.
- מפות קרנו ושרטוטים ותיקים מסוימים נשמרים גם ב־`source/fixed-visuals` כדי
  להבטיח תוצאה קבועה.

## RTL — מאין מגיע כיוון הכתיבה

ה־Word מספק את הטקסט והמבנה, אך האתר עצמו קובע RTL. בזמן הבנייה:

- פסקאות וטבלאות בעברית מסומנות RTL.
- נוסחאות, משתנים, אנגלית וקוד Arduino מבודדים LTR.
- תמונות מקבלות מידות קבועות כדי למנוע קפיצות בזמן טעינה.

לכן אין צורך להוסיף קוד RTL ב־Word. כן צריך לבדוק בדפדפן סוגריים, מספרים,
יחידות, נוסחאות ואנגלית אחרי כל שינוי.

## בנייה ב־Windows

התקנה חד־פעמית: Python 3, Pandoc ו־LibreOffice Writer. בזמן התקנת Python יש
לסמן `Add Python to PATH`.

אחר כך:

1. סוגרים את Word.
2. לוחצים פעמיים על `BUILD_SITE_WINDOWS.bat` כדי לבנות מהקובץ המצורף; או
   גוררים עליו DOCX חדש.
3. ממתינים להודעה `BUILD AND VALIDATION SUCCEEDED`.
4. הדפדפן נפתח. בודקים את כל השינויים.

אם מופיעה שגיאה, לא מפרסמים. מעתיקים את כל הודעת השגיאה למתחזק הטכני.

## בנייה ב־macOS או Linux

התקנה חד־פעמית של Python 3, Pandoc ו־LibreOffice, ואז מתוך תיקיית הפרויקט:

```bash
bash BUILD_SITE.sh
```

לבנייה מקובץ אחר:

```bash
bash BUILD_SITE.sh "/full/path/to/new-version.docx"
```

## רשימת בדיקה לפני פרסום

- מספר וכותרת השאלה נכונים.
- השאלה והפתרון לא התחלפו.
- כל הטקסט מופיע ורק הטקסט המיועד לפרסום מופיע.
- כל התמונות, הטבלאות והשרטוטים קיימים.
- עברית, אנגלית, מספרים ונוסחאות מוצגים בסדר הנכון.
- שאלות מרובות סעיפים נפתחות סעיף אחר סעיף.
- פלט הבדיקה מסתיים ב־`Validation passed`.
- קובץ `.docx` אינו מופיע בין קובצי Git המיועדים לפרסום.

## פרסום

הדרך האוטומטית ב־Bash:

```bash
bash PUBLISH_TO_GITHUB.sh
```

הסקריפט בונה ובודק שוב, משכפל את המאגר לתיקייה זמנית, מעתיק את הפרויקט בלי
קובצי Word, מבצע commit ודוחף ל־`main`. Git עשוי לבקש התחברות ל־GitHub.

אפשר גם להשתמש ב־GitHub Desktop: `Fetch/Pull`, להריץ בנייה, לבדוק שה־Word אינו
ברשימת השינויים, לבצע Commit ואז Push. אין צורך להעלות ZIP או להעלות ידנית את
כל הפרויקט; Git שולח רק את השינויים.

## אם מתקבלת גרסת Word חדשה

1. שומרים את ה־DOCX החדש כגיבוי.
2. מוודאים ששאלות חדשות הוכנסו לפי הכללים למעלה.
3. מחליפים את `private-source/Automation_book4Aug2026.docx`, או מוסרים את
   הנתיב החדש לסקריפט הבנייה.
4. בונים ומריצים בדיקה.
5. בודקים ידנית את השאלות החדשות ואת גבולות הסעיפים.
6. מפרסמים את `docs/` והקוד, אך לא את קובץ ה־Word.

## כלל הזהב

להעתיק תבנית קיימת מאותו סעיף, לשמור `פתרון` בפסקה נפרדת, לבנות, לבדוק ורק
אז לפרסם.

